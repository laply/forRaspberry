# os 단에서 얻어온 ip address와 
# 지정해 놓은 port를 사용해 전달하도록 수행 // 전달 받은 값으로 수행하도록 함 

import subprocess

class Carmera :

    def __init__(self):
        self.ip = subprocess.call('') ## ip
        
        # ip address 
        
        
        subprocess.call('') ##  카메라 실행 call 
        